/**
 * Helpers estáticos sin estado (métodos {@code static} puros, utilidades
 * de formato, extracción, serialización defensiva, etc.).
 * <p>
 * Ninguna clase de este paquete debe tener estado mutable propio ni
 * comportamiento que represente un objeto de negocio; en lo posible se evita
 * la instanciación (constructores privados / {@code @NoArgsConstructor} de
 * acceso {@code PRIVATE}).
 */
package mx.bastekor.flowweaver.util;